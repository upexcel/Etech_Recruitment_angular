describe('Settings/Users Log Page Test', function () {
    //it should visit setting/userLog page
    it('Visits the Setting/Users Log Page', function () { })

    //test there is a button for user list,which has cuurent users
    it('check page for users list button ,having current logged-in user', function () { })

    //check if a new added user gets updated on userlist or not
    it('test new addd user is updated in user list pop-up', function () { })

    //click on add users list button, it should open 'pop up list' to select a user.
    //check if a user selected from list ,pop-up disappears and shown at user email 
    it('test select users functionality of user list button', function () { })
    
    //check if user activity is registerd in user activity table or not
    it('user activity table gets updated on some activity or not', function () { })
})
