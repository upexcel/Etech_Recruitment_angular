describe('Setting/Email Variable Page Test', function () {
    //it should visit setting/smtp page
    it('Visits the Setting/Email Variable Page', function () { })

    //test there is a button for add a new variable, also there is a list of system and user variable
    it('check page for add new variable button and list of system and user variable', function () { })

    //click on add new variable button, it should open 'Create Variable' popup to add a new variable.
    //check if add variable form is invalide then submit button must be disable.
    it('test add variable form with envalid data', function () { })

    //check if user fill variable name and select 'Add header/footer' option then a text box should be enable and if user select 'other than header/footer' option then a ckedito option must be enable
    it('test "Add header/footer" or "Other than header/footer" functionality', function () { })

    //check if user hit back button popup must be closed
    it('test back button functionality', function () { })

    //if user fill the form then submit button mus be enable. And if user hit this button a api must be fire, popup will close and this varibale should be listed in user variable list.
    it('text add new variable functionality', function () { })

    //test if user click on delete button from user list, it should open a confirm popup and user, it have 2 option 'yes' and 'no' buttons, if user click on 'no' popup will close and user variable list should be remain same, or if user hit yes variable should be deleted and user varibale list should be updated 
    it('text delete email varibale funtionality', function () { })
})