version: 2
jobs:
  build:
    docker:
      - image: node:8.6.0
    steps:
      - checkout
      - run: npm install
      - run: npm install pm2 -g
      - run: pm2 start npm --name hr-recruit -- start
      - run: pm2 logs 0
